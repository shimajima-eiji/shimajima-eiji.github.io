# 公開確認 — 2026-09-05 JST

実装コミット: `935cff02b053b79d7a654abc5a416379370982ad`。
後続変更は重複した同値のTypeScript設定キーの整理と検証用User-Agentの明示、本文書のみ。

- `npm run build`: 型検査とVite本番ビルド成功。
- `npm test`: 4件成功。固定局面でユーザー履歴のみを変え、選ばれる合法手が変化するA/Bを含む。
- `npm audit`: 修正版Vite 8.2.2導入後 0 vulnerabilities。
- `python3 scripts/validate.py`: 既存サイト・ICS自己テスト PASSED。
- `python3 tests/api-check.py`: ローカル成功。
- 同スクリプトの公開Workers URL指定: 成功。同意・CSRF・合法性・別ID隔離・独立Cookie jar間ログイン/途中再開・競合・待った・削除・全セッション失効。
- 公開入口 `/games/` HTTP200、ゲームURLのリンクを確認。プロフィールの `/games/` リンクも確認。
- 実ブラウザで公開URLの盤面表示、非ログイン e2→e4、コンピューター Nc6、同意後の「保存済み」、新しい盤面から2手の棋譜の再開を確認。
- 公開版の通信はアプリ側のno-storeヘッダーを確認。資格情報・棋譜本文をログ出力する処理はない。

初回Cloudflare version: `80bb33eb-ec66-4fe3-adf9-069e79020694`。
専用D1: `aba81da6-54cc-4c58-bf7d-1a50fd782be2`、対象アカウント `b7c850847e8b19ce4f034620e417b145`。

Python標準User-Agentは公開環境で403になったため、検証ツール名をUser-Agentに明示した。ブラウザとcurlはアクセス成功。サービス側のアクセス制御は変更していない。

限界: 本人の癖に応手が変わる機構を検証した。実ユーザーに適切な難易度になることや棋力向上は未実証。実端末2台の操作テストではなく、APIの独立Cookie jarで端末間共有を検証している。

# 大将棋追加 — 2026-09-05 JST

実装コミット `1ab8b1396737c7db88481c4e7dfa9174ee50166f`。
Cloudflare version `fc2debdd-9a3d-4b05-8816-8a63ee88f3b2`。

- `npm run build`、全13テスト（チェス4 + 大将棋9）、ルート `bash scripts/ci-local.sh` 成功。
- 大将棋は全駒の空盤移動数、方向・遮蔽・跳躍、獅子の二枚取り/居喰い/パス、角鷹/飛鷲の方向制限、成り・不成・再成り禁止、太子を含む王駒捕獲、反復禁止を検証。
- 同じ盤面で個人履歴のみを変えるA/Bで、大将棋コンピューターの合法手選択が変化することを検証。
- 生成した検証棋譜350手の再生36ms、初期局面AI 8ms（このMacのNode測定）。本番上限性能の保証ではない。
- 既存チェスAPIチェック（ローカル）成功。
- 大将棋APIチェック（ローカル・本番）成功。別ユーザー隔離、チェス/大将棋の隔離、競合、不正手、独立Cookie jar間の途中対局共有、ゲーム別削除、共通アカウント失効。
- ブラウザで歩兵の移動とAI応手、獅子の味方越え跳躍、パス、4手の保存と再開をローカルで確認。
- 公開版で歩兵の移動・AI応手・保存を確認。大将棋とチェスの公開HTMLはローカルビルドとのハッシュ一致。
- `/games/` 公開入口に大将棋リンクあり、HTTP200。

ゲームルールの異説と採用裁定は `src/dai/RULES.md` と対局画面に明示。実ユーザーの棋力への適合・学習効果は未実証。


## 2026-09-05 全対局アーカイブ

- archive-v1 同意画面、全対局の各保存時点の非公開アーカイブ、90日失効の対象外、削除時の参照解除を実装。旧方針データは同意して再開するまで移行しない。
- `npm run build`、`npm test`（14件）、ルート `scripts/ci-local.sh` 成功。SQLite実トリガーで途中・待った・競合時非追加・旧データ失効・本人削除後の保持を検証。
- ローカルのチェス／大将棋API検証成功。旧クライアントの同意なし保存を428で拒否。ブラウザで同意→歩兵8k→8j→AI応手→2手保存→待ったを確認。
- 本番D1 migration 0003 適用済み。本番 `dai-api-check.py` 成功。専用テストIDのチェス／大将棋を保存し、履歴・アカウントを削除した後、アーカイブは0件→4件、4件すべて game_id=NULL を確認。実利用者の棋譜は取得していない。
- 公開ブラウザで保存・削除時保持の同意文面を読み戻した。管理取得手順は ARCHIVE.md。
- 通信未到達の手、既に削除・上書きされた旧棋譜は保証範囲外。別系統バックアップと管理画面は未実装。
